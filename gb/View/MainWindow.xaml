<Window x:Class="gb.View.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gb.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Title="Finish Rooms Floors" Height="450" Width="800"
             Background="#2C3E50">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="731*"/>
            <ColumnDefinition Width="69*"/>
        </Grid.ColumnDefinitions>
        <!-- Background Circle -->
        <Ellipse Fill="#34495E" Width="400" Height="400" VerticalAlignment="Center" HorizontalAlignment="Left" Opacity="0.2" Margin="200,0,0,0"/>

        <!-- Main Content -->
        <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Margin="260,0,0,0">
            <TextBlock Text="Finish Rooms Floors" FontSize="30" FontWeight="Bold" Foreground="#ECF0F1" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <!-- Finish Rooms Floors Button -->
            <Button Content="Finish Rooms Floors" Width="200" Height="50" FontSize="16" FontWeight="Bold" 
                    Background="#1ABC9C" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Padding="10,5" Click="FinishRoomsFloorsButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25" 
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#1ABC9C"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Setter Property="OverridesDefaultStyle" Value="True"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#16A085"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#148F77"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
